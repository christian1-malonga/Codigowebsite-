import { packages } from '../data/siteContent';
import { OrangeButton } from '../components/SiteShell';
import { SectionHeading, SectionWrap } from '../components/SectionComponents';

export function PricingSection() {
  return <SectionWrap tone="soft" watermark="PRICING" className="pricing-section" reveal><SectionHeading title="Delivery" italic="options" intro={<><strong>Clear pricing</strong> with no surprise fees. Every package is available in USD and AED</>} /><div className="pricing-grid">{packages.map(item => <article className={`pricing-card ${item.recommended ? 'pricing-card--recommended' : ''}`} key={item.number}>{item.recommended && <span className="recommended-badge">TOP PICK</span>}<div className="pricing-card__top"><div className="pricing-meta"><span className="mono">{item.number} / Plan</span><span>{item.duration}</span></div><h3>{item.title}</h3><p>{item.copy}</p><div className="pricing-price"><span>From</span><strong>{item.price}</strong>{item.aed && <small>{item.aed}</small>}</div></div><div className="pricing-includes"><span className="meta-label">What’s included</span><ul>{item.includes.map(include => <li key={include}>{include}</li>)}</ul></div></article>)}</div><div className="pricing-footer"><p className="pricing-note">Prices are indicative starting points. Final scope and cost are confirmed after a free discovery call. We invoice in USD or AED — VAT applies where required</p><OrangeButton href="/contacts">Request pricing</OrangeButton></div></SectionWrap>;
}
