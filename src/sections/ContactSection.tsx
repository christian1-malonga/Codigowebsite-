import { FormEvent } from 'react';
import { OrangeButton } from '../components/SiteShell';
import { Field, SectionWrap, TextField } from '../components/SectionComponents';

export function ContactSection({ compact = false, reveal = true }: { compact?: boolean; reveal?: boolean }) {
  const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); window.alert('Thanks — our team will reply within one business day.'); };
  return <SectionWrap id={compact ? undefined : 'contact'} tone="orange" dark watermark="CONTACT" className={`contact-section ${compact ? 'contact-section--compact' : ''}`} reveal={reveal}><div className="contact-heading"><h2>Share your<br /><span className="contact-project-line">product <em>vision</em></span></h2><p>Our team will respond within <strong>one business day</strong></p></div><div className="contact-grid"><form className="contact-form" onSubmit={submit}><div className="contact-form__row"><Field label="01 — Name" placeholder="Jane Doe" required /><Field label="02 — Organisation" placeholder="Acme Inc" /></div><Field label="03 — Email address" placeholder="you@company.com" type="email" required /><TextField label="04 — Project overview" placeholder="A short description of what you want to build" /><div className="contact-field"><label>05 — Budget range (if known)</label><select defaultValue=""><option value="" disabled>Choose a range</option><option>Under $25k</option><option>$25k – $75k</option><option>$75k – $150k</option><option>$150k+</option></select></div><div className="contact-submit-wrap"><OrangeButton type="submit" className="contact-submit">Submit enquiry</OrangeButton></div></form><div className="direct-contact"><span>Or contact us directly</span><small>Email</small><a href="mailto:hello@codigo.com"><span>hello@codigo.com</span><span className="direct-arrow" aria-hidden="true">→</span></a></div></div></SectionWrap>;
}
